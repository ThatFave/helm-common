1. Get the application URL by running these commands:
   export POD_NAME=$(kubectl get pods --namespace default -l "app=common-template" -o jsonpath="{.items[0].metadata.name}")
   echo "Visit http://127.0.0.1:8080 to use your application"
2. To enable Tailscale ingress, set tailscaleIngress.enabled: true in your values.yaml and configure the required settings.